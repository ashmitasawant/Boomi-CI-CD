name: Deployment Pipeline

on:
  push:
    paths:
     - 'DeploymentConfiguration/*'
  workflow_dispatch:

jobs:
  getEnvironmentInfo:
   runs-on: ubuntu-latest
  
   steps:
   - name: Sample Azure action
     env: # Or as an environment variable
      credentials: ${{ secrets.BOOMI_AUTHORIZATION }}
     run: echo ${{env.credentials}}
