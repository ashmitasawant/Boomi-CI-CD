name: Deployment Pipeline

on:
  push:
    paths:
     - 'DeploymentConfiguration/*'
  workflow_dispatch:
env: 
   ASHMITA_ACCOUNTID: ${{secrets.ASHMITA_ACCOUNTID}}
jobs:
  getEnvironmentInfo:
   runs-on: ubuntu-latest
  
   steps:
   - name: Sample Azure action
     
     run: echo $ASHMITA_ACCOUNTID
  get:
   runs-on: ubuntu-latest
   steps:
   - name: Sample Azure action
     
     run: echo $ASHMITA_ACCOUNTID
