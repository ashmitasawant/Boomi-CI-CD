name: Deployment Pipeline

on:
  push:
    paths:
     - 'DeploymentConfiguration/*'
  workflow_dispatch:
env: 
 AUTHAPITOKEN: ${{secrets.AUTHAPITOKEN}}
jobs:
  getEnvironmentInfo:
   runs-on: ubuntu-latest
  
   steps:
   - name: Sample Azure action
    
     run: echo ${{env.AUTHAPITOKEN}}
  get:
   runs-on: ubuntu-latest
   steps:
   - name: Sample Azure action
    
     run: echo ${{env.AUTHAPITOKEN}}
