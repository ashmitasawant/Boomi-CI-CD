name: test
on: workflow_dispatch
env: 
   ASHMITA_SAWANT_ACCOUNTID: ${{secrets.ASHMITA_SAWANT_ACCOUNTID}}
jobs:
  job1:
   runs-on: ubuntu-latest
   outputs:
    secret-number: ${{steps.sets-a-secret.outputs.secret-number}}
   steps:
      - uses: actions/checkout@v2
      - id: sets-a-secret
        run: |
              git log https://github.com/ashmitasawant/Boomi-CI-CD/blob/6b1f94b422191222952711002f09836876ed77c8/DeploymentConfiguration/ProcessDetails.Crow_Demo.dev.json

              

              
